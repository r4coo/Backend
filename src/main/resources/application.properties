server.port=${PORT:8080}
server.servlet.context-path=/api

# PostgreSQL Railway
spring.datasource.url=jdbc:postgresql://shortline.proxy.rlwy.net:40846/railway?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=mpOlDAygFYIDzxUeDwuGcoVtQMVlMUXP
spring.datasource.driver-class-name=org.postgresql.Driver

# SSL para Railway
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Desactiva H2
spring.h2.console.enabled=false

# JWT
jwt.secret=${JWT_SECRET:valorant-secret-key-change-this-in-production}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}